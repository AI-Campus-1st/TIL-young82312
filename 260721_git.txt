git - 글 관리 프로그램 (올리는 것까지)

최초 작성자(Author) 설정
github - 커뮤니티 (게시판)
git- 글 관리 프로그램 (올리는 것)

git config --global user.name "닉네임"
git config --global user.email "github 이메일"

최초 1번만 실행
git clone 저장소 주소(복제)
이후엔 git pull 사용